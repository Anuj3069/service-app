/**
 * src/modules/match/index.js — Match Module Public API
 */

const matchRoutes = require('./match.routes');
const matchService = require('./match.service');

module.exports = { matchRoutes, matchService };
